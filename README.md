# first.repository
first program to give user input and making some chainges
pienv\fastapi==0.115.2
uvicorn==0.31.0

# ORM & Database
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9   # PostgreSQL driver for Supabase

# Security & Password Hashing
passlib==1.7.4
argon2-cffi==23.1.0

# Data validation
pydantic==2.7.4

# (Optional but useful)
python-dotenv==1.0.1     # for environment variables (DB URL, secrets)

# JWT Authentication (if you add tokens later)
PyJWT==2.9.0
